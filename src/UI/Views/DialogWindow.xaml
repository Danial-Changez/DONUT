<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dialog"
        SizeToContent="Height"
        Width="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="{DynamicResource PrimaryBackground}"
        AllowsTransparency="False"
        ResizeMode="NoResize">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <!-- Window-scoped ScrollViewer template: the stock template fills the
             corner where the vertical and horizontal scrollbars meet with a
             SystemColors-painted Rectangle, which shows up as a white box at the
             bottom of the update list. Re-template it so that corner cell stays
             empty/transparent. Scoped to this window so it can't override the
             TextBox PART_ContentHost ScrollViewers elsewhere in the app. -->
        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Row="0"
                                                    Grid.Column="0"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Margin="{TemplateBinding Padding}" />

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Row="0"
                                       Grid.Column="1"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="1"
                                       Grid.Column="0"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />

                            <!-- Grid.Row="1" Grid.Column="1" (the corner) is left
                                 intentionally empty so no white box is drawn. -->
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border CornerRadius="0"
            BorderThickness="1"
            BorderBrush="{DynamicResource PanelBorder}">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0"
                                 EndPoint="1,0.75">
                <GradientStop Color="{DynamicResource PrimaryBackgroundDarkColor}"
                              Offset="0" />
                <GradientStop Color="{DynamicResource PrimaryBackgroundColor}"
                              Offset="1" />
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- Header (drag region: title + X) -->
                <RowDefinition Height="Auto" />
                <!-- Message -->
                <RowDefinition Height="Auto" />
                <!-- List -->
                <RowDefinition Height="Auto" />
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Header (popup chrome contract): 48px row, the whole row is the drag region;
                 X pinned Top with its flush 50x36 pad (square - the window is square), the
                 title centered beside it with top air. Esc also closes (DialogPresenter). -->
            <Grid x:Name="panelControlBar" Grid.Row="0" Background="Transparent" Height="48">
                <TextBlock Text="{Binding Title}"
                           Visibility="{Binding HasTitle, Converter={StaticResource BoolToVis}}"
                           Style="{DynamicResource TextPaneTitle}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="24,0,56,0"
                           TextTrimming="CharacterEllipsis"
                           ToolTip="{Binding Title}" />
                <Button x:Name="btnClose"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Height="36"
                        Style="{DynamicResource controlButton}"
                        Tag="{DynamicResource Destructive}"
                        ToolTip="Close">
                    <Path Style="{DynamicResource controlButtonIcon}"
                          Data="{DynamicResource xMark}" />
                </Button>
            </Grid>

            <!-- Message (left-aligned, muted) -->
            <TextBlock Grid.Row="1"
                       Text="{Binding Message}"
                       Visibility="{Binding HasMessage, Converter={StaticResource BoolToVis}}"
                       Style="{DynamicResource TextBody}"
                       Margin="24,6,24,0"
                       TextWrapping="Wrap" />

            <!-- List: each item a subtle muted card (mono, for paths/versions) -->
            <ScrollViewer Grid.Row="2"
                          Visibility="{Binding HasList, Converter={StaticResource BoolToVis}}"
                          MaxHeight="168" VerticalScrollBarVisibility="Auto"
                          Margin="24,12,24,0">
                <ItemsControl x:Name="lstContent" ItemsSource="{Binding ListItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceMuted}"
                                    BorderBrush="{DynamicResource PanelBorder}" BorderThickness="1"
                                    CornerRadius="6" Padding="10,6" Margin="0,0,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Left}"
                                               Foreground="{DynamicResource BodyTextPrimary}"
                                               FontFamily="{DynamicResource FontMono}" FontSize="12"
                                               TextWrapping="Wrap" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding Right}"
                                               Foreground="{DynamicResource BodyTextSecondary}"
                                               FontFamily="{DynamicResource FontMono}" FontSize="12"
                                               HorizontalAlignment="Right" VerticalAlignment="Center"
                                               Margin="12,0,0,0" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Buttons: bottom-right, secondary then primary (primary rightmost). The primary
                 binds its Style from the VM (accent, or destructive-red for irreversible acts). -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="24,22,24,20">
                <Button x:Name="btnSecondary"
                        Content="{Binding SecondaryText}"
                        Command="{Binding SecondaryCommand}"
                        Visibility="{Binding HasSecondary, Converter={StaticResource BoolToVis}}"
                        MinWidth="96" Height="36" Margin="0,0,8,0"
                        Style="{DynamicResource ButtonSecondary}" />
                <Button x:Name="btnPrimary"
                        Content="{Binding PrimaryText}"
                        Command="{Binding PrimaryCommand}"
                        MinWidth="96" Height="36"
                        Style="{Binding PrimaryStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>