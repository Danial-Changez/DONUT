<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="controlButton"
           TargetType="Button">
        <Setter Property="Width"
                Value="50" />
        <Setter Property="Background"
                Value="Transparent" />
        <Setter Property="Foreground"
                Value="{DynamicResource BodyTextTertiary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <!-- Square hover pad: these buttons sit flush in window corners. -->
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver"
                     Value="True">
                <Setter Property="Background"
                        Value="{DynamicResource PanelBackgroundActive}" />
                <Setter Property="Foreground"
                        Value="{Binding Path=Tag, RelativeSource={RelativeSource Self}}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- Radius 9 is the card's 10 minus its 1px border, so the pad sits flush in the corner. -->
    <Style x:Key="controlButtonCardClose"
           TargetType="Button"
           BasedOn="{StaticResource controlButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="0,9,0,0">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Arcane button system, ported from getarcaneapp/arcane at its 14px root scale.
         Shared: radius 8.4, height 32, padding 14, 12.5px medium, disabled opacity 0.5. -->
    <Style x:Key="ButtonBase"
           TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TitleTextPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusRingVisual}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize" Value="12.5" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- Variants restyle Background and borders via Style.Triggers on hover. -->
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8.4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ButtonSecondary"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource Secondary}" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryForeground}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SecondaryHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ButtonOutline"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PanelBackgroundHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ButtonGhost"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Foreground" Value="{DynamicResource TitleTextTertiary}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PanelBackgroundHover}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleTextPrimary}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- ButtonSecondary with pill padding. Not the ghost variant, ButtonGhost is that. -->
    <Style x:Key="ButtonPill"
           TargetType="Button"
           BasedOn="{StaticResource ButtonSecondary}">
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Padding" Value="14,7" />
        <Setter Property="FontSize" Value="12" />
    </Style>
    <!-- BadgeMuted's geometry as a button, for a badge that is also an action. -->
    <Style x:Key="ButtonBadge"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource SurfaceMuted}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="FontSize" Value="11" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource PanelBackgroundActive}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- Tint recipe: fill at 10 percent of the status colour, border at 30, hover lifts both. -->
    <!-- Text is near-white, not violet, so the primary action reads deep rather than loud. -->
    <Style x:Key="ButtonTintPrimary"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource TintViolet}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TintVioletBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TintVioletHover}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TintVioletBorderHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ButtonTintSuccess"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource TintGreen}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentGreen}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TintGreenBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TintGreenHover}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TintGreenBorderHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ButtonTintDestructive"
           TargetType="Button"
           BasedOn="{StaticResource ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource TintRed}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentRed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TintRedBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TintRedHover}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TintRedBorderHover}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- Dim when there is nothing to clear and destructive on hover, so it encodes its state. -->
    <Style x:Key="fieldClearButton"
           TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource BodyTextTertiary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="22" />
        <Setter Property="Height" Value="22" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusRingVisual}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                        <Path x:Name="glyph"
                              Data="{DynamicResource xMark}"
                              Width="9"
                              Height="9"
                              Stretch="Uniform"
                              StrokeThickness="0.75"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              FlowDirection="LeftToRight"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Stroke="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="glyph" Property="Stroke" Value="{DynamicResource Destructive}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="glyph" Property="Opacity" Value="0.35" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
