<!-- Container and text-role styles. Colours and fonts live in UIColors.xaml, and everything
     here is consumed via DynamicResource, so merge order does not matter. -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- WPF's default dotted adorner is invisible on near-black (WCAG 2.4.7). -->
    <Style x:Key="FocusRingVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="-3"
                               StrokeThickness="2"
                               Stroke="{DynamicResource Primary}"
                               RadiusX="9"
                               RadiusY="9"
                               SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Instances still set Margin, and override Padding where a denser card is wanted. -->
    <Style x:Key="Card"
           TargetType="Border">
        <Setter Property="Background"
                Value="{DynamicResource PanelBackground}" />
        <Setter Property="BorderBrush"
                Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="CornerRadius"
                Value="10" />
        <Setter Property="Padding"
                Value="16,14" />
    </Style>

    <!-- The centered card every overlay pops. Instances set Width and x:Name only. -->
    <Style x:Key="OverlayCard"
           TargetType="Border">
        <Setter Property="Background"
                Value="{DynamicResource PrimaryBackground}" />
        <Setter Property="BorderBrush"
                Value="{DynamicResource PanelBorder}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="CornerRadius"
                Value="10" />
        <Setter Property="HorizontalAlignment"
                Value="Center" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <!-- Focusable so opening an overlay can scope its Esc key binding to the card. -->
        <Setter Property="Focusable"
                Value="True" />
    </Style>

    <!-- Text roles on one ladder (11, 12, 13, 14, 16, 18), so views never size text inline. -->
    <!-- The tier above TextTitle, used for pane and dialog headings. -->
    <Style x:Key="TextPaneTitle"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="18" />
        <Setter Property="FontWeight"
                Value="Bold" />
        <Setter Property="Foreground"
                Value="{DynamicResource TitleTextPrimary}" />
    </Style>
    <Style x:Key="TextTitle"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="16" />
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="Foreground"
                Value="{DynamicResource TitleTextPrimary}" />
    </Style>
    <Style x:Key="TextSubtitle"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="14" />
        <Setter Property="FontWeight"
                Value="SemiBold" />
        <Setter Property="Foreground"
                Value="{DynamicResource TitleTextPrimary}" />
    </Style>
    <Style x:Key="TextBody"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="13" />
        <Setter Property="Foreground"
                Value="{DynamicResource BodyTextSecondary}" />
    </Style>
    <!-- The line under a title: a subtitle, hint, or secondary fact. -->
    <Style x:Key="TextMeta"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="Foreground"
                Value="{DynamicResource BodyTextTertiary}" />
    </Style>
    <!-- The smallest tier, for stamps and trailing facts beside a value. -->
    <Style x:Key="TextCaption"
           TargetType="TextBlock">
        <Setter Property="FontFamily"
                Value="{DynamicResource FontSans}" />
        <Setter Property="FontSize"
                Value="11" />
        <Setter Property="Foreground"
                Value="{DynamicResource BodyTextTertiary}" />
    </Style>
</ResourceDictionary>
